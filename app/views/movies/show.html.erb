<h2> Movie Details are.....</h2>
<h3><%= @movie.name %></h3>
<p><%= @movie.rating %></p>
<p><%= @movie.description %></p>
<p><%= @movie.release_date %></p>

<% if user_signed_in? %>
  <% if current_user.role == 'admin' %>
  <%= link_to 'Edit', edit_movie_path%>
  <% end %>
<% end %>
<%= link_to 'Show Reviews', movie_reviews_path(@movie.id) %>

<%= link_to ' Write Reviews', new_movie_review_path(@movie.id) %>
